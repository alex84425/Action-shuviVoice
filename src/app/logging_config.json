{
  "logger": {
    "filepath": "/data/log/debug.log",
    "format": "<green>{time:YYYY-MM-DD HH:mm:ss.SSS}</green> [{extra[task_id]}] <level>{level: <5}</level> <level>{message}</level> - <cyan>{file}:{line}</cyan>",
    "level": "debug",
    "retention": "1 months",
    "rotation": "1 days"
  }
}