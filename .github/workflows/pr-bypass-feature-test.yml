name: Bypass Feature Test for BOT

on:
    pull_request:
        branches: [master]

jobs:
    reuse:
        uses: vcosmos-actions/reusable-workflows/.github/workflows/template-bypass-feature-test.yml@master
        secrets: inherit
