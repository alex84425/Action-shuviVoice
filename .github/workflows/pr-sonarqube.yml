name: Quality Check On PR

on:
    pull_request:
        branches: [master]

jobs:
    call-template-sonarqube:
        uses: vcosmos-actions/reusable-workflows/.github/workflows/template-sonarqube.yml@master
        secrets: inherit
