name: PR Code Fixer

on:
    workflow_dispatch:
    pull_request:
        branches: [master]

jobs:
    call-pr-code-fixer:
        uses: vcosmos-actions/reuse-pr-code-fixer/.github/workflows/pr-code-fixer.yml@v1.0.0
        secrets: inherit
