name: Pre-commit Updater

on:
    workflow_dispatch:
    schedule:
        - cron: "0 0 * * *"

jobs:
    call-template-pre-commit-updater:
        uses: vcosmos-actions/reusable-workflows/.github/workflows/template-pre-commit-updater.yml@master
        secrets: inherit
